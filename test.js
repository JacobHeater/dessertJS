/**
 * Adds the two numbers and returns the sum.
 * 
 * @param {Number} a The first number.
 * @param {Number} b The second number.
 * 
 * @returns {Number} The sum of the two numbers added.
 */
function add(a, b) {
    return a + b;
}