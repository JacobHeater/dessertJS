(() => {

    'use strict';

    define(main);

    function main() {
        return addController;
    }

    function addController(app) {
        app.controller('jquery-example-controller', function (page) {
            
        });
    }

})();